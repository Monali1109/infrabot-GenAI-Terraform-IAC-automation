# ============================================
# Terraform Provider Configuration
# Generated by Infra-Bot
# ============================================

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 5.0"
    }
  }
}

provider "aws" {
  region = "{{ region | default('us-west-1') }}"
}
